import React from 'react';
import { Box, Typography } from '@mui/material';

export default function InternalHome() {
    return (
        <Box>
            <Typography>Internal Home Page</Typography>
            <Typography>Eventually we can use Nextjs web analytics to put site info here</Typography>
        </Box>
    );
}